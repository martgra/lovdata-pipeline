"""Example environment configuration.

Copy this file to .env and adjust values as needed.
"""

# ============================================================================
# Dataset Sync Configuration
# ============================================================================

# Dataset filter pattern (default: 'gjeldende')
# Options:
#   'gjeldende' - Both laws and regulations (full dataset)
#   'gjeldende-lover' - Only laws (~1/5 size, recommended for testing)
#   'gjeldende-sentrale-forskrifter' - Only central regulations
LOVDATA_DATASET_FILTER=gjeldende

# Directory paths
LOVDATA_RAW_DATA_DIR=./data/raw
LOVDATA_EXTRACTED_DATA_DIR=./data/extracted
LOVDATA_STATE_FILE=./data/state.json

# Download concurrency (1-10, default: 4)
LOVDATA_MAX_DOWNLOAD_CONCURRENCY=4

# ============================================================================
# Chunking Configuration
# ============================================================================

# Maximum tokens per chunk (default: 6800)
LOVDATA_CHUNK_MAX_TOKENS=6800

# Chunk output file path
LOVDATA_CHUNK_OUTPUT_PATH=./data/chunks/legal_chunks.jsonl

# Force reprocessing of all files (default: false)
LOVDATA_FORCE_REPROCESS=false

# ============================================================================
# Embedding Configuration
# ============================================================================

# OpenAI API key (REQUIRED for embeddings)
LOVDATA_OPENAI_API_KEY=sk-your-api-key-here

# OpenAI embedding model (default: text-embedding-3-large)
# Options: text-embedding-3-large, text-embedding-3-small, text-embedding-ada-002
LOVDATA_EMBEDDING_MODEL=text-embedding-3-large

# Batch size for embedding API calls (1-2048, default: 100)
LOVDATA_EMBEDDING_BATCH_SIZE=100

# Directory for enriched chunks with embeddings
LOVDATA_ENRICHED_DATA_DIR=./data/enriched

# Force re-embedding of all files (default: false)
# Useful when upgrading models or changing embedding logic
LOVDATA_FORCE_REEMBED=false

# ============================================================================
# Vector Database Configuration
# ============================================================================

# Vector database type (default: chroma)
# Note: Only 'chroma' is currently supported
LOVDATA_VECTOR_DB_TYPE=chroma

# Collection/index name for vector database (default: legal_docs)
LOVDATA_VECTOR_DB_COLLECTION=legal_docs

# ----------------------------------------------------------------------------
# ChromaDB Settings
# ----------------------------------------------------------------------------

# Deployment mode (default: persistent)
# Options:
#   - memory: In-memory storage (fast, data lost on restart)
#   - persistent: Local disk storage (data persists)
#   - client: Connect to remote ChromaDB server
LOVDATA_CHROMA_MODE=persistent

# Client mode settings (only used when LOVDATA_CHROMA_MODE=client)
LOVDATA_CHROMA_HOST=localhost
LOVDATA_CHROMA_PORT=8000

# Persistent mode settings (only used when LOVDATA_CHROMA_MODE=persistent)
LOVDATA_CHROMA_PERSIST_DIRECTORY=./data/chroma

# ----------------------------------------------------------------------------
# Pinecone Settings (used when LOVDATA_VECTOR_DB_TYPE=pinecone)
# ----------------------------------------------------------------------------

# Pinecone API key (REQUIRED for Pinecone)
# LOVDATA_PINECONE_API_KEY=your-pinecone-api-key

# Pinecone environment (e.g., gcp-starter, us-east-1-aws)
# LOVDATA_PINECONE_ENVIRONMENT=gcp-starter

# Pinecone index name
# LOVDATA_PINECONE_INDEX_NAME=lovdata

# ----------------------------------------------------------------------------
# Weaviate Settings (used when LOVDATA_VECTOR_DB_TYPE=weaviate)
# ----------------------------------------------------------------------------

# Weaviate instance URL (REQUIRED for Weaviate)
# LOVDATA_WEAVIATE_URL=http://localhost:8080

# Weaviate API key (if authentication enabled)
# LOVDATA_WEAVIATE_API_KEY=your-weaviate-api-key

# Weaviate class name for documents
# LOVDATA_WEAVIATE_CLASS_NAME=LegalDocument

# ----------------------------------------------------------------------------
# Qdrant Settings (used when LOVDATA_VECTOR_DB_TYPE=qdrant)
# ----------------------------------------------------------------------------

# Qdrant instance URL (REQUIRED for Qdrant)
# LOVDATA_QDRANT_URL=http://localhost:6333

# Qdrant API key (if authentication enabled)
# LOVDATA_QDRANT_API_KEY=your-qdrant-api-key

# Qdrant collection name
# LOVDATA_QDRANT_COLLECTION_NAME=legal_docs

# ============================================================================
# Pipeline Manifest
# ============================================================================

# Path to pipeline manifest file
LOVDATA_PIPELINE_MANIFEST_PATH=./data/manifest.json
